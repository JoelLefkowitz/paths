#ifndef TEST_PATHS_ROOT_H
#define TEST_PATHS_ROOT_H

#include <string>
#include <vector>

struct RootTestCase {
    std::string path;
    std::string expected;
};

const std::vector<RootTestCase> cases = {};

#endif
