#ifndef TEST_PATHS_TAIL_H
#define TEST_PATHS_TAIL_H

#include <string>
#include <vector>

struct TailTestCase {
    std::string path;
    std::string expected;
};

const std::vector<TailTestCase> cases = {};

#endif
